<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.work">

    <!-- To auto-complete the email text field in the login form with the user's emails -->
    <uses-permission android:name="android.permission.GET_ACCOUNTS" />
    <uses-permission android:name="android.permission.READ_PROFILE" />
    <uses-permission android:name="android.permission.READ_CONTACTS" />
    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/icon"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.AppCompat.DayNight.NoActionBar">
        <activity
            android:name=".MainActivity"
            android:label="@string/app_name"></activity>
        <activity android:name=".RegisterActivity" />
        <activity android:name=".LoginActivity">
        </activity>
        <activity android:name=".CalendarActivity" />
        <activity android:name=".PaperBankActivity" />
        <activity android:name=".QuestionActivity" />
        <activity android:name=".MyWordBankActivity" />
        <activity android:name=".MyDataActivity" />
        <activity android:name=".PaperWordListActivity" />
        <activity android:name=".QuestionPaperreviewActivity" />
        <activity android:name=".MyPaperBankActivity" />
        <activity android:name=".QuestionReviewActivity" />
        <activity android:name=".StartPicActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>